// Phase 0 stub. Full implementation (live phase indicator, live charts, ABORT,
// ImpactReport) lands in Phase 7 (spec §11.3).
export default function RunScreen() {
  return (
    <section className="screen">
      <h1>Run &amp; result</h1>
      <p className="muted">
        Live phases, metric charts, and the impact report appear here during an
        experiment.
      </p>
    </section>
  );
}
